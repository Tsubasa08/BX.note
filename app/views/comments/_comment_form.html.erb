<% if logged_in? %>

  <%= form_with(model: Comment.new(id: current_user.id)) do |f| %>
  <%= f.text_area :content, id: 'comment-textarea' %>
  <%= hidden_field_tag 'comment[post_id]', post.id %>
  <%= f.submit "送信", id: 'comment-submit' %>
  <% end %>
  
<% else %>
  <p>ログインしてください</p>
<% end %>