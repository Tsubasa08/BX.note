<%  if current_user.image.attached? %>
  <%= image_tag current_user.image.variant(resize: "200x200"), class: name, id: id %>
<% else %>
  <img id="<%= id %>" class="<%= name %>" src="<%= asset_pack_path 'media/src/images/defoult-user-icon.png' %>" />
  
<% end %>