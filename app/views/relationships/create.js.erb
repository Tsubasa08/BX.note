$(function() {
  $("#follow_form-<%= @user.id %>").html("<%= j(render('users/unfollow', user: @user, user_page: @now_user)) %>");
  $(".link__num--followers").html("<%= @now_user.followers.count %>");
});